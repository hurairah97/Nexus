

@{
    ViewBag.Title = "Nexus | Products";
}
<head>
    <style>
        #cen{
            text-align: center;
            
               
        }
    </style>
</head>
<section class="page-banner-area page-service">
    <div class="overlay"></div>
    <!-- Content -->
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-9 col-md-12 col-12 text-center">
                <div class="page-banner-content">
                    <h1 class="display-4 font-weight-bold">products are also available</h1>
                    <p>We'd love to talk about how we can help you.</p>
                </div>
            </div>
        </div> <!-- / .row -->
    </div> <!-- / .container -->
</section>
<section class="section" id="services-2">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <p class="pl-3 text-white">
                    Want to know
                    more about this? just leave us message <a href="@Url.Action("Contact","Home")" class="text-warning">Contact us</a>
                </p>
            </div>
        </div> <div class="row
justify-content-center">
            <div class="col-md-8 col-lg-6 text-center">
                <div class="section-heading">
                    <!-- Heading --> <h2 class="section-title mb-2
text-white">
                        Services
                    </h2>

                    <!-- Subheading --> <p class="mb-5 text-white">

                        Products are available according to needs.

                    </p>
                </div>
            </div>
        </div> <!-- / .row -->

        <div class="row">
            @foreach (var item in ViewBag.a)
            {
                <div id="cen" class="col-lg-4 col-sm-6 col-md-6 mb-30">
                    <div class="web-service-block">
                        @if (@item.id == 1)
                        {
                            <img src="~/Content/admin/images/router.jpg" alt="Girl in a jacket" width="200" height="150" >} 
                         @if (@item.id == 2)
                        {
                            <img src="~/Content/admin/images/modem.jpg" alt="Girl in a jacket" width="200" height="150">}   <h3>
    <span>@item.name</span><br />
    <span>@item.price $</span>
</h3>
                                <p>
                                    @if (@item.id == 1)
                                    {
                                        <span> modem is a box that connects your home network to the wider Internet</span>}
                                    @if (@item.id == 2)
                                    {
                                        <span> A router is a box that lets all of your wired and wireless devices use that Internet connection at once.</span>}

                                    .
                                </p>

                                @if (Session["c_id"] != null)
                                {
                                    @Html.ActionLink("Order Now", "prorder", "Home", new { id = item.id }, null)
                                }
                                else
                                {
                                    @Html.ActionLink("Order Now", "Login", "Home", new { id = item.id }, null)
                                }




                        </div>
                </div>
            }

        </div>
    </div>
</section>


